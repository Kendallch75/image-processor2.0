# Cambios completados

- Se eliminó el placeholder de `aplicarFiltro/4`.
- Cada trabajador Erlang ejecuta una instancia Scheme independiente.
- Se implementó protocolo Erlang -> Scheme con región, halo, filtro, parámetro y tamaño de kernel.
- Scheme devuelve únicamente las filas de la región asignada.
- Se implementó detección de fallo de Scheme y un reintento automático.
- Se implementó timeout configurable.
- Se agregó Gaussian 3x3 y 5x5 con convolución genérica.
- Se conservaron Grayscale, Invert, Brightness, Threshold, Sharpen y Sobel.
- Se corrigió la carpeta `scheme ` a `scheme/` y el archivo a `image_processor.scm`.
- Se eliminaron los delimitadores Markdown del archivo Scheme.
- Se verificaron las restricciones Scheme: sin `if`, `set!`, `let`, `while` ni `cons`.
- Se mejoró la lectura PPM P3 para aceptar comentarios `#`.
- Se mejoró la división y el halo para kernels mayores y regiones pequeñas.
- Se agregó interfaz ejecutable `./image_processor`.
- Se agregó benchmark automático para 1, 2, 4 y 8 procesos.
- Se calculan tiempo, speedup y eficiencia y se exportan a CSV.
- Se agregaron tres imágenes PPM de prueba.
- Se agregó prueba de equivalencia del resultado Gaussian entre 1, 2, 4 y 8 procesos.
- Se agregó manual de ejecución y base del informe técnico.
